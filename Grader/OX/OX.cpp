#include <bits/stdc++.h>
using namespace std;

int main(){
    int s,n;
    char p;
    cin>>s>>n>>p;
    queue<char> in[2];
    

    for(n--){
        
        cin>>in[0]>>in[1];
        
    }
    
        int r,c;
        if(r<s&&c<s){
            if(ox[r][c] != )
        }
}